Torifier's fork ships with the spam filter already patched in. 

To activate this spam filter in the mainstram original pyBitmessage:

Manually exchange (replace) the file named "class_objectProcessor.py" with the version which torifier provides in the /spamfilter directory.
